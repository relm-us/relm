import { writable } from "svelte/store";

export const keyUp = writable(false);
export const keyDown = writable(false);
export const keyLeft = writable(false);
export const keyRight = writable(false);
